# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (never commit secrets; template stays in repo)
.env*
!.env.example

# claude code (local machine paths / permissions)
.claude/settings.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local scripts / temp
scripts/get-unis.mjs
scripts/list-unis.mjs

# sqlite db (contains real data, never commit)
prisma/*.db
prisma/*.db-journal
